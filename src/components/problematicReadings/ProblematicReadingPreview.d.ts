import type { Reading } from '../../types';
type ProblematicReadingPreviewProps = {
    problematicReading: Reading;
    idx: number;
};
export declare const ProblematicReadingPreview: ({ problematicReading, idx }: ProblematicReadingPreviewProps) => import("react/jsx-runtime").JSX.Element | undefined;
export {};
